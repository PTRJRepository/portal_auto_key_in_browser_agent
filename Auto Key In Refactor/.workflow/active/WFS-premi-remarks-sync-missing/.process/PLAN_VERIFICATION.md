# Plan Verification

## Scope Check

- Current repo scanned for `MISS`, `MISMATCH`, `sync:`, `match:`, `PREMI`, and `POTONGAN_KOTOR`.
- External `refactor_production` project scanned for `extend_db_ptrj`, `manualAdjustmentService.ts`, remarks parser, premium definitions, and sync-status logic.
- Existing workflow sessions from last seven days checked; no direct conflict found, but `month-filter-bug` may overlap `api_client.py` / `main_window.py` if still uncommitted.

## Key Assumptions

- `remarks` tokens are generated by backend seeding/sync logic and should be trusted before local verification.
- `sync:MISS` means source/manual adjustment row has not been input to Plantware ADTRANS yet.
- `match:MISMATCH` means input is still needed or amount needs correction, unless user explicitly disables mismatch processing.
- `KOREKSI PANEN` should use `POTONGAN_KOTOR` path and `DE0004` / `(DE) POTONGAN PREMI` metadata.

## Go/No-Go

- Quality gate: `PROCEED_WITH_CAUTION`.
- Must inspect `git status --short` before implementation because this repo has parallel-tool safety rules.
- Must add tests before changing eligibility logic because wrong filtering can input already-synced payroll rows.
